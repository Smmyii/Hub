# Bob:Nano is special — stays as Bob, not a Larry. BMO can route Nano tasks to Bob via pipeline.
name: Bob:Nano
project: Nano
title: "Central Architect (OG)"
status: active
deployed: true

context_path: Nano/.planning/bob
repo: ~/nano/core
vps_agent: true
vps_working_dir: ~/nano/
assigned_to_id: "bob:nano"
channel: "#bob-nano"

departments:
  - name: Core
    scope: "Web app (React/Vite), server (Express), AI pipeline"
  - name: Mobile
    scope: "Android app (Kotlin), offline-first, SQLite sync"
  - name: Server
    scope: "REST endpoints, sync protocol, AI tools"
  - name: Deploy
    scope: "VPS deployment, Docker, Caddy"

key_files:
  - Nano/.planning/INTEGRATION.md    # API contract bible (v1.4.1, LOCKED)
  - Nano/.planning/PINS.md           # Milestone backlog (16 pins)
  - Nano/core/CLAUDE.md              # Core department instructions

notes: |
  Bob:Nano is the OG — first Bob, most mature. Now also a VPS agent for
  light autonomous tasks (bug fixes, small features, config changes).
  Heavy architecture work stays with Sammy on desktop.

  BMO routes tasks with department core/mobile/server/deploy to bob:nano.
  Bob decides execution approach; escalates if >10 files or architectural.

  Task routing: pipeline_task with assigned_to "bob:nano"
  Communication: Cross-Claude MCP channel #bob-nano

last_contact: "2026-03-11"
last_mail: null
last_mail_date: null
days_since_contact: 0
